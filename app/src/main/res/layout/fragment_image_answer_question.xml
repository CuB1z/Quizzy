<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".fragments.ImageAnswerQuestionFragment">

    <!-- Área para el texto de la pregunta -->
    <TextView
        android:id="@+id/questionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:textSize="18sp"
        android:gravity="center" />

    <!-- Área cuadrada para las imágenes, usando GridLayout para selección -->
    <GridLayout
        android:id="@+id/imageGrid"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:columnCount="2"
        android:rowCount="2"
        android:layout_margin="16dp"
        android:background="@android:color/darker_gray">

        <!-- Ejemplo de ImageView; repetir o generar dinámicamente -->
        <ImageButton
            android:id="@+id/image1"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_columnWeight="1"
            android:layout_rowWeight="1"
            android:src="@drawable/ic_launcher_foreground"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <ImageButton
            android:id="@+id/image2"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_columnWeight="1"
            android:layout_rowWeight="1"
            android:src="@drawable/ic_launcher_foreground"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <ImageButton
            android:id="@+id/image3"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_columnWeight="1"
            android:layout_rowWeight="1"
            android:src="@drawable/ic_launcher_foreground"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <ImageButton
            android:id="@+id/image4"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_columnWeight="1"
            android:layout_rowWeight="1"
            android:src="@drawable/ic_launcher_foreground"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

    </GridLayout>

</LinearLayout>